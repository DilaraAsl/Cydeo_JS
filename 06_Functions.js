// no access modifier, no method overloading
// non return type
function add(a,b) { 
    console.log("Total is : "+(a+b));
    
}
// return type 
add(10,20);
function name(a,b) {
    return (a*b);
}
console.log(multiply(10,20));